
import styled from 'styled-components';

export const ChooseStyled = styled.section`
    margin-top: 15rem;
    min-height: 30vh;
    background-color: #fff;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(20rem, 1fr));
 
  
    
    grid-gap: 6rem;
`;

